<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
  <!-- Kept: network (Supabase / Sentry / Garmin / InAppBrowser); Notifee POST_NOTIFICATIONS; haptics VIBRATE -->
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <!--
    Strip merges unused by app code (evidence 2026-08-13):
    - WRITE_EXTERNAL_STORAGE: react-native-fs merge; export writes CachesDirectoryPath + Share FileProvider only
    - USE_BIOMETRIC / USE_FINGERPRINT: react-native-keychain merge; SecureStore uses WHEN_UNLOCKED only (no biometric gate)
    Removed app declarations (no code path): RECORD_AUDIO, READ_EXTERNAL_STORAGE, MODIFY_AUDIO_SETTINGS,
    SYSTEM_ALERT_WINDOW (debug overlay stays in src/debug*), SCHEDULE_EXACT_ALARM (Notifee timer uses
    TIMESTAMP without alarmManager → WorkManager default)
  -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
  <uses-permission android:name="android.permission.USE_BIOMETRIC" tools:node="remove"/>
  <uses-permission android:name="android.permission.USE_FINGERPRINT" tools:node="remove"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:enableOnBackInvokedCallback="false">
    <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <activity
        android:name=".MainActivity"
        android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.App.SplashScreen"
        android:exported="true"
        android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <!-- Production custom scheme (Garmin OAuth kinefit://garmin-callback, etc.) -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="kinefit"/>
      </intent-filter>
      <!-- Smoke deep links (host form): kinefit://smoke/{auth|tabs|seed|clear} -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="kinefit" android:host="smoke" android:pathPrefix="/auth"/>
        <data android:scheme="kinefit" android:host="smoke" android:pathPrefix="/tabs"/>
        <data android:scheme="kinefit" android:host="smoke" android:pathPrefix="/seed"/>
        <data android:scheme="kinefit" android:host="smoke" android:pathPrefix="/clear"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
